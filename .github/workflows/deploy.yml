name: build-and-deploy

on:
  push:
    branches:
      - develop

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout latest code
        uses: actions/checkout@v3

      - name: Install Composer dependencies (theme only)
        run: composer install --no-interaction --prefer-dist --optimize-autoloader

      - name: Install npm packages & build frontend (theme)
        run: |
          npm install
          npm run build

      - name: Sync theme to VPS (development)
        run: |
          rsync -avz --delete \
            -e "ssh -o StrictHostKeyChecking=no -i ~/.ssh/id_rsa" \
            ./ developmentrevof@43.250.141.151:/home/developmentrevof/public_html/wp-content/themes/startdigital
        env:
          SSH_PRIVATE_KEY: ${{ secrets.SSH_PRIVATE_KEY }}

      - name: Done
        run: echo "✅ Theme deployed to development server."
